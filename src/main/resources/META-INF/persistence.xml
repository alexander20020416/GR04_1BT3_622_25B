<?xml version="1.0" encoding="UTF-8"?>
<persistence xmlns="http://xmlns.jcp.org/xml/ns/persistence"
             xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
             xsi:schemaLocation="http://xmlns.jcp.org/xml/ns/persistence
             http://xmlns.jcp.org/xml/ns/persistence/persistence_2_1.xsd"
             version="2.1">

    <persistence-unit name="GestorTareasPU" transaction-type="RESOURCE_LOCAL">
        <provider>org.hibernate.jpa.HibernatePersistenceProvider</provider>

        <!-- Entidades -->
        <class>com.gr4.model.Actividad</class>
        <class>com.gr4.model.Tarea</class>
        <class>com.gr4.model.Alerta</class>
        <class>com.gr4.auth.model.UsuarioEntity</class>

        <properties>
            <!-- Configuración de H2 Database -->
            <property name="javax.persistence.jdbc.driver" value="org.h2.Driver"/>

            <!-- Modo FILE: Los datos persisten entre reinicios -->
            <property name="javax.persistence.jdbc.url" value="jdbc:h2:file:./data/gestor_tareas"/>

            <!-- Modo MEMORY: Los datos se borran al cerrar (útil para pruebas) -->
            <!-- <property name="javax.persistence.jdbc.url" value="jdbc:h2:mem:gestor_tareas;DB_CLOSE_DELAY=-1"/> -->

            <property name="javax.persistence.jdbc.user" value="sa"/>
            <property name="javax.persistence.jdbc.password" value=""/>

            <!-- Configuración de Hibernate -->
            <property name="hibernate.dialect" value="org.hibernate.dialect.H2Dialect"/>

            <!-- update: Actualiza el esquema sin borrar datos (MANTIENE USUARIOS)
                 create: Crea las tablas cada vez (borra datos existentes)
                 validate: Solo valida que las tablas existan
                 create-drop: Crea al inicio y borra al cerrar -->
            <property name="hibernate.hbm2ddl.auto" value="update"/>
            
            <!-- Reiniciar secuencias al iniciar -->
            <property name="hibernate.id.new_generator_mappings" value="true"/>
            <property name="hibernate.hbm2ddl.import_files_sql_extractor" value="org.hibernate.tool.hbm2ddl.MultipleLinesSqlCommandExtractor"/>

            <property name="hibernate.show_sql" value="true"/>
            <property name="hibernate.format_sql" value="true"/>
            <property name="hibernate.use_sql_comments" value="true"/>

            <!-- Pool de conexiones -->
            <property name="hibernate.connection.pool_size" value="5"/>

            <!-- Caché -->
            <property name="hibernate.cache.use_second_level_cache" value="false"/>

            <!-- Configuración adicional de H2 -->
            <property name="hibernate.connection.autocommit" value="false"/>
            
            <!-- Configuración adicional para compatibilidad -->
            <property name="hibernate.archive.autodetection" value="class"/>
            <property name="hibernate.enable_lazy_load_no_trans" value="true"/>
        </properties>
    </persistence-unit>
</persistence>